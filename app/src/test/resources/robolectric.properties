# The real EhApplication.onCreate registers broadcast receivers and starts device-only services that
# throw on a desktop JVM (DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION). None of the JVM unit tests need
# it, so boot a stub Application instead. Per-test code that does need the app can still override this.
application=android.app.Application
